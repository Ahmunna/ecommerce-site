
window.onload = () => {
    
    var buttons = document.getElementsByClassName('cartButton');
    console.log(buttons);
    //Array.from(buttons).forEach((element) => element.addEventListener('click',() => console.log("heart")));
}